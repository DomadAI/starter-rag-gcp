# Example environment variables for running Domad AI's RAG Starter Kit

# Vector store settings
VECTORSTORE_TYPE=faiss          # Options: faiss (default), chroma (future)
VECTORSTORE_PATH=vectorstore    # Where vector DB is saved/loaded

# Indexing behavior
INDEXING_MODE=chunk             # Options: chunk (default), summary (Pro), hybrid (Pro)

# RAG response settings
STRICT_MODE=true                # If true, fallback when match is weak or unrelated
MIN_RESULTS=1                   # Minimum matching docs required to trigger LLM response
PROMPT_STYLE=strict             # Options: strict, verbose, default

# Embedding model provider
EMBEDDING_MODEL=OpenAI          # Options: OpenAI, HuggingFace
OPENAI_API_KEY=your-openai-api-key

# Project details
PROJECT_NAME=DomadRAGDemo

# Optional: GCP deployment settings
FIRESTORE_PROJECT_ID=your-gcp-project-id
GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json
